{
  "name": "parallel-story-builder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd mobile && expo start",
    "lint": "cd mobile && eslint .",
    "clean": "cd mobile && rm -rf node_modules .expo"
  }
}
